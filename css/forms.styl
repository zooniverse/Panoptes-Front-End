.form-label
  @extends $generic-heading

.auto-save
  &[data-waiting]
    background: rgba(gray, 0.05)

  &[data-busy]
    background-stripes: rgba(gray, 0.3), transparent, 1em
    animation: busy-animation 1s linear infinite

  &[data-success]
    background: rgba(lime, 0.2)

  &[data-error]
    background: rgba(red, 0.2)

.standard-input
  border: 1px solid
  border-radius: 2px
  box-shadow: 1px 2px 5px -1px rgba(black, 0.2) inset
  box-sizing: border-box
  font: inherit
  padding: 0.5em 1vw

  &:disabled
    opacity: 0.5

  &.full
    display: block
    width: 100%

.form-help
  color: #646464

  &.info
    font-style: italic

  &.error
    color: red

  &.warning
    color: orangered

  &.success
    color: green

  &.right-align
    text-align: right

.form-separator
  display: block
  margin-bottom: 1em

select.multiline-select
  width: 100%
  margin-top: 1em
  margin-bottom: 1em
